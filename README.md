# hash-attack

Two types of attacks are implemented (collision attack, pre-image attack) and tested on 8, 10, 16, 20, 24 bits.
The expected time complexity and the actual time complexities for both collision attack(2^(n/2)) and pre-image attack(2^n) are shown as logs, tables and graphs.
